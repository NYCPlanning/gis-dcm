[PATHS]
log_path = Path to log files
translator = Path to ESRI metadata xml translator
xslt_html = Path to ESRI metadata stylesheet
xslt_lcl_strg = Path to ESRI local storage metadata GPTool
xslt_geoproc_hist = Path to ESRI geoprocessing history metadata GPTool
sde_trd_path = Path to TRD SDE
sde_prod_path = Path to Production SDE
lyr_dcm_path = Path to DCM layer file folder
export_path = Path to temp local directory
production_export_path = Path to Bytes Production folder
template_path = Path to templates
city_map_alteration_path = Path to city map alteration files

[VARS]
publication_date = YYYYMMDD
sde_user_disconnect = Bool var for disconnecting users